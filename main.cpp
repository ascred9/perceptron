#include "perceptron.h"

int main()
{
    // Perceptron prc(/*...*/);
    // ReadData(filename, vec_x, vec_y);
    // Perceptron.Train(vec_x, vec_y, 0.7);
    // Perceptron.Test(vec_x, vec_y);
    // Save test result and then draw it by python matplot
    return 1;
}